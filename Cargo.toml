[package]
name = "lightdom-test"
version = "0.1.0"
edition = "2024"

[dependencies]
scraper = "0.20"
async-trait = "0.1"
anyhow = "1.0"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
